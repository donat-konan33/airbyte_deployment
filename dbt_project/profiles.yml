
dbt_project:
  outputs:
    dev:
      method: thrift
      schema: default  # ou ton schéma de travail
      host: spark-master
      port: 10000
      user:  "{{ enr_var('USER') }}"
      database: "{{ env_var('DATABASE') }}"
      connect_timeout: 10
      retries: 1
      type: spark
  target: dev
